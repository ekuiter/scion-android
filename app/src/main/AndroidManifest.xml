<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.scionlab.endhost">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:extractNativeLibs="true">
        <service
            android:name=".DispatcherService"
            android:exported="false"
            android:process=":DispatcherService" />
        <service
            android:name=".SciondService"
            android:exported="false"
            android:process=":SciondService" />
        <service
            android:name=".ScmpService"
            android:exported="false"
            android:process=":ScmpService" />
        <service android:name=".ScionService"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HelpActivity"/>
        <activity android:name=".AboutActivity"/>
    </application>

</manifest>